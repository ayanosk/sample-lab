# project_a

個別プロジェクトの最小構成サンプルです。

## フォルダの役割

- `sources/` : 元データ
- `scripts/` : 処理用コード
- `outputs/` : 生成物
- `docs/` : メモ、下書き、作業ログ

## 使い方

- 元データは `sources/` に置いたままにする
- 処理は `scripts/` で行い、結果は `outputs/` に出す
- 気づいたことや判断理由は `docs/` に残す
